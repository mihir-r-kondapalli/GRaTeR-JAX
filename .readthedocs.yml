version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "mambaforge-22.9"
  jobs:
    pre_build:
      - sphinx-apidoc -o docs/api grater_jax -e -f

conda:
  environment: environment.yml

python:
  install:
    - method: pip
      path: .

sphinx:
  configuration: docs/conf.py